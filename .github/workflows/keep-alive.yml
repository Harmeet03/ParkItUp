name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # Runs every 10 minutes
  workflow_dispatch:   # <-- THIS ENABLES MANUAL RUNS

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          curl -I https://parkitup-parking-booking-app.onrender.com/health
